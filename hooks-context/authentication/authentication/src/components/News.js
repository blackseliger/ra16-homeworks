import React, { useState, useUserInfo } from 'react';

function News({token}) {

    // const [news, setNews] = useState();

    // const [{data}] = useUserInfo(`${process.env.REACT_APP_AUTH_URL}/private/news`);
    // console.log(data)
    // // if (data && (JSON.stringify(news) !== JSON.stringify(data))) setNews(data);
    // console.log(news);

    return (
        <div>
            
        </div>
    )
}

export default News
